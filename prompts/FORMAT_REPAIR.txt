# FORMAT REPAIR

The last response missed the routing format and did not end with a valid route-map JSON object.

Reply briefly, then end with one fenced JSON object.

Example:
{"DEV":"message to DEV"}

With a handoff request:
{"DEV":"message to DEV","command":"handoff"}

Use route names as keys.
Do not use target, reason, or message wrapper keys.
